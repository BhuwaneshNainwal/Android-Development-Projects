<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cpcontest">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application android:label= "@string/app_name"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CPContest">
        <activity android:name=".SplashActivity" android:label="CPfier" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" android:label="CPfier">
        </activity>

        <activity android:name=".contest_item" android:exported="false" android:label="ALL"></activity>

        <activity android:name=".About" android:exported="false" android:label="ABOUT"></activity>

        <activity android:name=".HelpActivity" android:exported="false" android:label="HELP"></activity>

        <receiver android:name=".ReminderBroadcast"  android:exported="false"></receiver>

    </application>


</manifest>